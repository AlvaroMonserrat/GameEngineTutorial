#ifndef GAMEMAP_H
#define GAMEMAP_H

#include <vector>
#include "Layer.h"

class GameMap
{
    public:
        void Render()
        {
            for(unsigned int = 0; i < m_MapLayers.size(); i++)
            {

            }
        }

    private:
        std::vector<Layer*> m_MapLayers;
};

#endif // GAMEMAP_H
